# AGENTS

This repository keeps canonical agent operating guidance in `docs/maintainers/AGENTS.md`.

Start there first, then use:

- `docs/maintainers/AGENT-CLI-MAP.md` for command/approval tiering and copy-paste `workspace-kit` invocations.
- `.ai/AGENTS.md` for generated machine-readable mirrors aligned to the same policy.
