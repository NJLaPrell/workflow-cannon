# CANNON.md — project memory index

Human-curated operational memory for Workflow Cannon sessions. Machine records live in `.workspace-kit/memory/records.json`; use `pnpm exec wk run list-memory '{}'` and `explain-memory-precedence` for the governed merge story.

**Boundary:** This file does not override `.ai/` policy, task-engine state, or generated README/docs bodies.
