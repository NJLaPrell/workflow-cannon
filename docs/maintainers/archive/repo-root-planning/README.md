# Archived repo-root planning Markdown

**Moved from the repository root** in Phase 96 (**T100196**) so the default tree stays small: fewer accidental full-file loads in agents and cleaner GitHub root views.

## Current intentional root Markdown

Keep using these at repo root:

| File | Role |
| --- | --- |
| `README.md` | Landing page (documentation module–owned body; edit via `.ai/README.md` + templates). |
| `AGENTS.md` | Pointer entry for agents vs maintainers. |
| `CONTRIBUTING.md` | Contributor setup, checks, and phase journal regression pointer. |
| `CHANGELOG.md` | Release history. |
| `PHASE_JOURNAL.md` | Phase journal **operator contract** and example workflow (still referenced by `CONTRIBUTING.md`, tests, and task `planRef` strings). |
| `LICENSE` | License (not Markdown). |

## What landed here

Historical plans, transcripts, host-specific scratch, and one-off phase write-ups that used to live next to `README.md`. Treat as **read-only provenance**; execution truth is task-engine state, `.ai/` machine canon, and `docs/maintainers/` maintainer prose per **DOCUMENTATION-LIFECYCLE.md**.

If you need a former filename, search this directory — originals were moved with `git mv` (history preserved).
