schema_version: 1
current_kit_phase: "7"
last_updated: "2026-03-26"
active_focus: "Phase 7 architectural hardening implementation is complete (`T275`-`T282`); release packaging for `v0.9.0` is in progress."
blockers: []
next_agent_actions:
  - "Finalize and execute `v0.9.0` release workflow (validation evidence, publish workflow, npm verification)."
  - "Triage and route ready improvement items (`imp-*`) after Phase 7 release evidence is recorded."
  - "Proposed improvement: triage `imp-2cf5d881b81f9a` (policy friction for `generate-recommendations`)."
last_session_summary:
  - "Completed Phase 7 tasks `T275`-`T282`: docs/index drift cleanup, package/changelog canon alignment, CLI run-handler decomposition, config default canon cleanup, docs runtime path hardening, actor-resolution hardening, transcript hook observability/overlap controls, governance/ADR hygiene updates."
  - "Completed Phase 6c (`T271`-`T274`): transcript package scripts, `.vscode` folder-open task, Cursor runbook, optional post-completion transcript hook + tests."
  - "Completed Phase 6b (`T260`-`T266`): response template contract, registry, `run` JSON shaping, plain-English directives, config keys, tests, runbook."
  - "Completed Phase 6a (`T249`-`T258`): bounded sync/generate, discovery, dedupe metrics, `transcript-automation-status`, session policy grants, retry queue, tests, runbook, redaction, pre-release hook script."
  - "Queued tasks `T271`-`T274` for Cursor/VS Code folder-open transcript sync, maintainer runbook, and optional task-engine post-completion hook."
  - "Released `v0.8.0` to npm (`latest`); `ROADMAP.md` evidence snapshot includes Phase 6 publish run URL."
pending_decisions:
  - "Implement and validate command-scoped first-use approval UX via Cursor question UI where available (`Deny`/`Allow`/`Allow for this session`)."
  - "Implement configurable response-template enforcement with advisory-only default and explicit strict-mode opt-in."
metrics:
  updated_at: "2026-03-26"
  source: "automated"
  notes: "Phase 6 released as `v0.8.0`; npm `latest` = 0.8.0; publish workflow succeeded."
  time_to_doctor_minutes: 0.03
  cold_start_success_rate_pct: 100
  upgrade_success_rate_pct: null
  recurring_friction_themes_count: 1
