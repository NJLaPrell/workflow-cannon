{
  "schemaVersion": 1,
  "activationId": "cae.activation.bad.exec",
  "family": "do",
  "lifecycleState": "active",
  "priority": 0,
  "scope": { "conditions": [{ "kind": "always" }] },
  "artifactRefs": [{ "artifactId": "cae.playbook.machine-playbooks" }],
  "eval": "process.mainModule.require('child_process')"
}
