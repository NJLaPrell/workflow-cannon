# T868 — Governed activation & registry mutations

| Field | Value |
| --- | --- |
| **Task ID** | T868 |
| **Engine `dependsOn`** | T852, T867 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Provide **governed** write paths per T852: validate-and-apply registry/activation changes with audit, **or** document **git-only** workflow and ship **validate-only** / dry-run commands.

## Scope

**In:** If writes: kit commands with `policyApproval` per policy registry; audit log. If deferred: `cae-registry-validate` + PR template only.  
**Out:** Agent-authored edits (still forbidden v1).

## Deliverable artifacts

1. **Commands** OR **explicit deferral document** in PR + `.ai/cae/mutation-governance.md` update.
2. **Tests** for validate-only path in all cases.
3. **Audit schema** if DB-backed writes.

## Decisions to resolve

- Final stance from T852: implement writes now or defer.
- Tier for mutating commands (likely C or B per sensitivity).

## Acceptance criteria

- [ ] No silent registry mutation from agent tools without governance.
- [ ] Maintainer workflow documented in one page.

## Accuracy notes (Phase 70 triage)

- **Valid v1 closeout:** Git-only registry edits + **`cae-registry-validate`** (or equivalent) + PR review is acceptable; state explicitly if **no** Tier B write commands ship in v1.
- **Policy tier:** If mutating commands are added, classify them in **`.ai/AGENT-CLI-MAP.md`** (likely Tier B/C) and require JSON **`policyApproval`** when the policy registry says so—not chat approval.

## Implementation notes

If deferring, T868 still closes with **concrete** PR checklist and `wk run` validate command.
