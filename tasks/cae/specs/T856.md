# T856 — Future cognitive-map integration contract

| Field | Value |
| --- | --- |
| **Task ID** | T856 |
| **Engine `dependsOn`** | T839, T840, T842 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

**Non-binding** forward contract: reserved **`cognitive-map`** artifact type, nullable **`mapSignals`** on context, **`think`** family references to map ids — **no v1 runtime dependency**.

## Scope

**In:** Short ADR appendix or `.ai/cae/future-cognitive-maps.md`; explicit v1 ignore/validate rules.  
**Out:** Map implementation.

## Deliverable artifacts

1. **Contract doc** with field names and schemaVersion bump strategy when maps land.
2. **Validator behavior**: reject vs allow-inert for `cognitive-map` type (must match T839).
3. **Example** “future” context JSON with `mapSignals: null`.

## Decisions to resolve

- Field names (`mapSignals` vs `cognitiveMapRef`).
- Whether think→map link is **artifact id only** or typed edge.

## Acceptance criteria

- [ ] T842 and T840 remain implementable without maps.
- [ ] No code in repo **requires** map files to exist after this task.

## Implementation notes

This task is **documentation-only**; optional stub types in TS allowed if behind `never` or feature flag — avoid dead imports in hot path.
