# T852 — Activation CRUD mutation governance

| Field | Value |
| --- | --- |
| **Task ID** | T852 |
| **Engine `dependsOn`** | T841, T845 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Define **who** may mutate activations/registry: maintainer-only, audit trail, PR vs CLI, **no agent self-service** in v1; tie to `policyApproval` tier if kit commands mutate state.

## Scope

**In:** Governance model, audit fields, validation gates on write, deferral strategy (git-only workflow).  
**Out:** T868 implementation.

## Deliverable artifacts

1. **Governance doc** `.ai/cae/mutation-governance.md`.
2. **Threat model** short section: agent tampering, accidental broad scope.
3. **If git-only v1**: documented workflow “edit registry JSON + PR + validate command” — no DB writes.

## Decisions to resolve

- Git as SoT vs DB-writable definitions.
- Whether **`create-task`-class** `policyApproval` required for any future `cae-registry-apply` command.
- Audit log format (append-only JSONL vs DB table).

## Acceptance criteria

- [ ] T868 scope is either implemented commands **or** explicit “deferred; use PR workflow” with validate-only CLI.

## Implementation notes

Align with `.ai/PRINCIPLES.md` R009 (policy model changes need human approval).
