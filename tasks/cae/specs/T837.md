# T837 — CAE architecture & boundaries ADR

| Field | Value |
| --- | --- |
| **Task ID** | T837 |
| **Engine `dependsOn`** | _(none)_ |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Establish canonical boundaries: hard invariants **remain in code**; CAE may **tighten** agent behavior but **must not loosen** safety; define integration points with `workspace-kit`, task engine, module router, and **disambiguate** `ModuleActivationReport` (“module enablement”) from CAE “activations.”

## Scope

**In:** ADR document (location decided below), diagrams optional, explicit v1 non-goals, tables of “code owns / CAE owns / docs only.”  
**Out:** JSON Schemas, CLI names, SQLite DDL.

## Deliverable artifacts

1. **ADR file** (choose one path in decisions): e.g. `.ai/adrs/ADR-cae-architecture.md` *or* `docs/maintainers/adrs/ADR-cae-architecture.md` — must align with repo ADR conventions.
2. **Boundary checklist** (section in ADR): mapping each invariant class to owning layer (code vs CAE vs advisory).
3. **Integration sketch**: where CAE **read-only** hooks will eventually attach (pointer to `src/core/module-command-router.ts`, `src/cli.ts`).

## Decisions to resolve

- Canonical ADR path (`.ai/` vs `docs/maintainers/adrs/`).
- New kit **module id** name (e.g. `context-activation`) vs `src/core/cae/` only.
- Feature-flag strategy for rollout (`config` key name).
- Official user-facing name (“CAE” vs longer) for CLI copy.

## Acceptance criteria

- [ ] ADR merged; reviewers can answer “what CAE must never do” from one table.
- [ ] Every **omit-in-v1** item from program context is reflected as explicit non-goals.
- [ ] References to `.ai/PRINCIPLES.md` and `.ai/POLICY-APPROVAL.md` for approval vs acknowledgement separation (high level only).

## Implementation notes

Do **not** implement evaluation logic in this task — documentation only.

## Accuracy notes (Phase 70 triage)

- **ADR location:** Prefer **`.ai/adrs/`** (existing ADRs: `ADR-task-linked-checkpoints-v1.md`, etc.) so agents stay in machine canon; only use `docs/maintainers/adrs/` if maintainers explicitly require a human-twin path—then cross-link from `.ai/`.
- **Naming:** Call out **`ModuleActivationReport`** (`src/core/module-registry.ts`) vs **CAE activation** explicitly in a table; avoid overloading the word “activation” without qualifier.
