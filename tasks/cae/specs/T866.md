# T866 — Narrow CAE policy enforcement lane

| Field | Value |
| --- | --- |
| **Task ID** | T866 |
| **Engine `dependsOn`** | T851, T860, T862 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Implement **allowlisted** blocking behaviors from T851 after shadow bake; integrate with router so commands **fail** or **require** explicit acknowledgement / approval paths without weakening code gates.

## Scope

**In:** Enforcement hook in router (or wrapper), feature flag **separate** from shadow, tests for each allowlisted case, remediation catalog entries.  
**Out:** Broad policy DSL, CRUD.

## Deliverable artifacts

1. **Code** with allowlist table in one module (constant export).
2. **Tests** proving code-level `policyApproval` still required where applicable.
3. **`.ai/AGENT-CLI-MAP.md`** notes if new exit behavior affects operators.

## Decisions to resolve

- **Gate:** Enforcement merges only when **config flag** (name TBD) is on **and** allowlist matches T851; optional minimum **shadow** observation window documented in ADR (not calendar logic in code unless explicitly required).
- Exact allowlist contents (must match T851 doc).

## Acceptance criteria

- [ ] No enforcement path for non-allowlisted CAE outcomes.
- [ ] Failing tests if allowlist drift from T851 without doc update.

## Implementation notes

User asked to prompt if server changes break UI — here “UI” is CLI JSON consumers; call out breaking stdout changes in PR.
