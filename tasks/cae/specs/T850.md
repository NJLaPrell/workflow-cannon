# T850 — Advisory activation surfacing design

| Field | Value |
| --- | --- |
| **Task ID** | T850 |
| **Engine `dependsOn`** | T847, T849 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Design how **non-blocking** CAE outputs reach agents: `doctor --agent-instruction-surface`, **`buildAgentInstructionSurface`**, stderr structured logs, or dedicated payload fields.

## Scope

**In:** Payload shapes, size caps, degradation when CAE disabled or errors; backward compatibility / schemaVersion bumps.  
**Out:** Implementation (T865).

## Deliverable artifacts

1. **Design doc** with **before/after JSON** snippet for `AgentInstructionSurfacePayload` (or new sibling field `cae?: {...}`).
2. **Size budget**: max bytes; truncation strategy.
3. **Naming**: avoid confusion with `activationReport` — propose rename or nested namespace.

## Decisions to resolve

- Doctor-only vs instruction-surface vs both.
- Whether full bundle or **summary + traceId** reference only.
- Env kill-switch name.

## Acceptance criteria

- [ ] Advisory path cannot be mistaken for enforcement.
- [ ] Extension documented for `AGENT-CLI-MAP.md` payload description.

## Implementation notes

See `src/core/agent-instruction-surface.ts` and `.ai/AGENT-CLI-MAP.md` payload row.
