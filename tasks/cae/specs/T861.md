# T861 — Read-only CLI: list/get artifacts & activations

| Field | Value |
| --- | --- |
| **Task ID** | T861 |
| **Engine `dependsOn`** | T847, T857, T858 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Ship first **read-only** `workspace-kit run` commands for **registry inspection** — lowest risk path before evaluate.

## Scope

**In:** Module registration, `instructions/*.md`, handlers returning JSON per T847, tests invoking router or CLI.  
**Out:** evaluate/explain (T862).

## Deliverable artifacts

1. **Instruction markdown** files with Usage + Arguments (machine-operable).
2. **`AGENT-CLI-MAP.md`** copy-paste section updates.
3. **Integration tests** for JSON shape (`ok`, `code`, `data`).

## Decisions to resolve

- Final command names (must pass CLI map coverage check).
- Feature flags hiding CAE module when registry absent.

## Acceptance criteria

- [ ] `pnpm run check` passes including agent-cli-map coverage.
- [ ] Commands require **no** `policyApproval`.

## Implementation notes

Mirror patterns from `src/modules/documentation/index.ts` handlers and `ModuleCommandRouter` registration.
