# T869 — CAE integration test hardening

| Field | Value |
| --- | --- |
| **Task ID** | T869 |
| **Engine `dependsOn`** | T854, T861, T862, T864 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Expand **automated** coverage across CLI, router shadow hook, advisory payloads, registry drift, and failure modes — execute the **test plan** from T854.

## Scope

**In:** CI tests, fixture maintenance, flaky controls, regression for schema bumps.  
**Out:** New product features.

## Deliverable artifacts

1. **Test files** as enumerated in T854 plan (checklist closed in PR).
2. **CI config** updates if new test suite label needed.
3. **Coverage report** snippet or `c8` threshold note (if project uses it).

## Decisions to resolve

- Scope of E2E (full `pnpm run wk` spawn vs direct router).
- Whether to add **scheduled** job for large golden suite.

## Acceptance criteria

- [ ] At least one test proves **CAE failure does not bypass** code safety (T853 scenario).
- [ ] CLI map coverage remains green as commands grow.

## Implementation notes

Run `pnpm run check` and targeted test scripts before merge; iterate until green per user rules.
