# T855 — Operator documentation workflow (.ai-first)

| Field | Value |
| --- | --- |
| **Task ID** | T855 |
| **Engine `dependsOn`** | T847, T848 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Plan **operator** and **agent** docs under `.ai/` per routing rules: how to run read-only commands, read traces, interpret shadow, propose registry edits.

## Scope

**In:** Outline + stub files in `.ai/cae/` or `.ai/runbooks/`; cross-links to instruction paths.  
**Out:** Maintainer `docs/` twins unless explicitly required.

## Deliverable artifacts

1. **Doc outline** checked in: `tasks/cae/artifacts/operator-doc-outline.md` → migrated to `.ai/cae/README.md` (or similar) when written.
2. **Copy-paste blocks** for `AGENT-CLI-MAP.md` (may duplicate T847 — consolidate in implementation).
3. **“Debug CAE”** flowchart (markdown mermaid OK).

## Decisions to resolve

- Whether documentation module generates any CAE pages (`generate-document` scope).
- Single entrypoint file for agents vs split by role.

## Acceptance criteria

- [ ] New agent can find CAE from `.ai/AGENTS.md` or `.ai/cae/README.md` without reading `docs/maintainers/`.
- [ ] Doc routing rule respected (no routine dependency on `docs/` for execution).

## Implementation notes

See `.cursor/rules/agent-doc-routing.mdc`.
