# T859 — Implement CAE context builder

| Field | Value |
| --- | --- |
| **Task ID** | T859 |
| **Engine `dependsOn`** | T842 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Build **`EvaluationContext`** per T842 from task engine state, command name, workspace paths, config, governance signals — with **redaction** and **canonical serialization** for hashing.

## Scope

**In:** Builder API, unit tests with mocked task state, degradation per T853.  
**Out:** CLI (T862), runtime hook (T864).

## Deliverable artifacts

1. **Implementation** + **tests** with frozen golden context JSON (normalized paths).
2. **Adapter layer** documenting each field’s reader (task-engine command vs SQLite — per ADR).

## Decisions to resolve

- Whether builder runs in **Node** only or must be browser-free (likely CLI-only).
- Cache injection point (builder vs evaluator).

## Acceptance criteria

- [ ] No fields outside T842 contract without ADR amendment.
- [ ] Context builder never throws uncaught for “missing task” when command is global — defined behavior.

## Implementation notes

Read T853 matrix before choosing fail-open defaults for advisory surfaces.

## Accuracy notes (Phase 70 triage)

- **Degraded behavior:** Implement defaults per **T853** / `.ai/cae/failure-recovery.md` (when written)—do not silently invent context fields on partial failure.
