# T863 — Shadow mode in evaluate/explain pipeline

| Field | Value |
| --- | --- |
| **Task ID** | T863 |
| **Engine `dependsOn`** | T848, T860, T862 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Add **shadow** labeling to evaluate/explain per T848: **would activate / require / enforce** without blocking; same code path as live with mode flag.

## Scope

**In:** CLI flags or `mode` in JSON args, output fields, tests; optional persistence hooks (no-op if T867 deferred).  
**Out:** Runtime hook (T864), enforcement (T866).

## Deliverable artifacts

1. **Schema extension** merged into evaluate response envelope.
2. **Tests** comparing shadow vs live labels on fixture where enforcement would trigger (mock future hook).
3. **Operator snippet** for `.ai/cae/shadow-mode.md` cross-link updates.

## Decisions to resolve

- Flag names: `shadow: true` vs `mode: "shadow"`.
- Whether shadow adds **synthetic** trace events for “would enforce.”

## Acceptance criteria

- [ ] Shadow output does not mutate task store or registry.
- [ ] Documented in T847 contract file (update canonical).

## Implementation notes

Enforcement hook points should be **no-op** in shadow mode inside router until T866, but evaluate command can still **classify** would-enforce.
