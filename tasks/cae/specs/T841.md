# T841 — Activation lifecycle & versioning semantics

| Field | Value |
| --- | --- |
| **Task ID** | T841 |
| **Engine `dependsOn`** | T840 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Define **states** (draft / active / retired / disabled, exact enum TBD), transitions, evaluation eligibility (“disabled/retired do nothing”), and compatibility rules for schema bumps.

## Scope

**In:** State machine doc, gating rules for evaluation, deprecation policy.  
**Out:** Governed CRUD implementation (T868).

## Deliverable artifacts

1. **Normative doc** (section in ADR or standalone `.ai/cae/lifecycle.md`).
2. **State transition table** (from → to → who/what).
3. **Evaluator pre-filter spec**: order of filtering (retired before specificity, etc.) — must align with T843.

## Decisions to resolve

- Is **shadow-only** a lifecycle state or a **runtime flag** on `cae-evaluate`?
- Whether `draft` activations appear in production registries.
- Coexistence of multiple **definition** schema versions in one registry file.

## Acceptance criteria

- [ ] T860 implementer can filter candidates without interpretation gaps.
- [ ] “Retired/disabled do nothing” is unambiguous.

## Implementation notes

Coordinate with T852 (governance) for who may transition states.
