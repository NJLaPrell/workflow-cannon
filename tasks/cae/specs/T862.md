# T862 — Read-only CLI: evaluate, explain, health, conflicts, trace

| Field | Value |
| --- | --- |
| **Task ID** | T862 |
| **Engine `dependsOn`** | T846, T847, T859, T860 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Expose **evaluation**, **explanation**, **health**, **conflicts**, and **trace fetch** per T847/T846; remain **non-mutating**.

## Scope

**In:** Handlers, schemas for `data` payloads, tests; trace retrieval may be **ephemeral** until T867.  
**Out:** Shadow mode (T863).

## Deliverable artifacts

1. **Commands** + instructions for: evaluate, explain, health, conflicts, trace (exact names per T847).
2. **Round-trip test**: evaluate → `traceId` → explain (or inline explain path).
3. **Remediation hints** for common errors (optional tie-in to `cli-remediation.js`).

## Decisions to resolve

- `explain` input: traceId-only vs full re-run request body.
- Health definition: registry validity vs last successful eval timestamp.

## Acceptance criteria

- [ ] JSON outputs include `schemaVersion` where specified in T847.
- [ ] Large trace truncation matches T846 limits.

## Implementation notes

If traces ephemeral pre-T867, document in `data.trace.ephemeral: true`.
