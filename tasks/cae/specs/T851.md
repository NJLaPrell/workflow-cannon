# T851 — Narrow policy enforcement lane design

| Field | Value |
| --- | --- |
| **Task ID** | T851 |
| **Engine `dependsOn`** | T837, T843, T844 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Define **minimal allowlist** of CAE-driven outcomes that may **block** or **require** explicit human/JSON approval — strictly **subordinate** to code invariants and existing `policyApproval` lanes.

## Scope

**In:** Allowlist table, exit codes, remediation catalog entries plan (`cli-remediation` / error codes).  
**Out:** Implementation (T866).

## Deliverable artifacts

1. **Design doc** `.ai/cae/enforcement-lane.md` with **allowlist** and **explicit forbiddens**.
2. **Exit code / `code` string** mapping table for router.
3. **ADR sign-off** gate: enforcement ships only after shadow bake (duration TBD) — document flag.

## Decisions to resolve

- Ship T866 in same phase as shadow or **strictly after** time window.
- Whether CAE may require **additional** `policyApproval` fields vs only surface human steps.
- Interaction with `responseTemplate` / advisory enforcement mode.

## Acceptance criteria

- [ ] Every enforceable outcome traced to a code path sketch (router vs wrapper).
- [ ] “Fail closed” only for enumerated cases; everything else advisory by default.

## Implementation notes

Read `.ai/POLICY-APPROVAL.md` and `src/core/module-command-router.ts` policy checks before finalizing.
