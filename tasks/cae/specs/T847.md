# T847 — Read-only CAE CLI command contract

| Field | Value |
| --- | --- |
| **Task ID** | T847 |
| **Engine `dependsOn`** | T843, T846 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Normative **command names**, **JSON args**, **response envelopes**, exit codes, and **`AGENT-CLI-MAP.md`** registration obligations for read-only CAE commands.

## Scope

**In:** Contract doc + example payloads for every read-only command; policy tier (expect Tier C, no `policyApproval`).  
**Out:** Handlers (T861, T862).

## Deliverable artifacts

1. **Replace stub** [`../artifacts/stub-read-only-cli-contract.md`](../artifacts/stub-read-only-cli-contract.md) with final **`tasks/cae/artifacts/cae-read-only-cli-contract.v1.md`** (or move to `.ai/cae/cli-read-only.md`).
2. **JSON Schema** for each command’s `data` payload (or OpenAPI-style appendix).
3. **AGENT-CLI-MAP.md** edit checklist (which section gets copy-paste blocks) — apply in T861/T862 or here if small.

## Decisions to resolve

- `workspace-kit run <cmd>` naming: `cae-*` vs `context-activation-*` vs module-prefixed.
- Pagination / `cursor` for list commands.
- Human **table** output vs JSON-only (kit pattern).

## Acceptance criteria

- [ ] An agent can call every command from copy-paste JSON only.
- [ ] `pnpm run check` CLI map coverage plan documented (exclusions file if needed).

## Implementation notes

See `scripts/check-agent-cli-map-coverage.mjs` — new commands must be mapped or excluded with rationale.

## Accuracy notes (Phase 70 triage)

- **Module id:** Final commands are almost certainly `workspace-kit run <command>` via a dedicated module (e.g. `context-activation.*`); document the **full operation id** for each command for `AGENT-CLI-MAP.md` and exclusions JSON if any command is internal-only.
- **Human output:** If table mode exists, specify that **agents should use JSON** (`--json` or kit default for automation) so parsers do not break.
