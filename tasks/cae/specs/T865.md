# T865 — Advisory CAE payload surfacing

| Field | Value |
| --- | --- |
| **Task ID** | T865 |
| **Engine `dependsOn`** | T850, T864 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Attach **summary** CAE bundle / trace reference to **doctor** and/or **agent instruction surface** per T850 — **non-blocking**, size-bounded.

## Scope

**In:** Extend `buildAgentInstructionSurface` payload and/or doctor output; snapshot tests; schemaVersion bump if needed.  
**Out:** Enforcement.

## Deliverable artifacts

1. **Payload extension** with backward compatibility strategy documented.
2. **Tests** in `test/` for JSON shape and size bound.
3. **AGENT-CLI-MAP.md** row update describing new `data` fields.

## Decisions to resolve

- Field name: `caeSummary` vs nested `contextActivation`.
- Whether to rename `activationReport` (breaking) vs add sibling (non-breaking).

## Acceptance criteria

- [ ] When CAE disabled, surface unchanged or explicit empty `cae` block per design.
- [ ] No full novel-length bundle dumped by default.

## Implementation notes

Coordinate with `src/core/agent-instruction-surface.ts` and CLI doctor path in `src/cli.ts`.
