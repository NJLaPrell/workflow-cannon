# T867 — CAE persistence: traces, retention, migrations

| Field | Value |
| --- | --- |
| **Task ID** | T867 |
| **Engine `dependsOn`** | T845, T846, T862 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Implement storage per T845: persist **traces** (and optional shadow metrics / ack state), migrations, read APIs for **`cae-activation-trace`**.

## Scope

**In:** DDL/migrations, repository layer, wire T862 trace command to DB, retention job or lazy prune.  
**Out:** CRUD for activations (T868).

## Deliverable artifacts

1. **Migration files** or kit command `cae-migrate` (follow repo patterns).
2. **Repository** with integration tests (sqlite temp file).
3. **Doctor** line for CAE storage health.

## Decisions to resolve

- If T845 chose **ephemeral v1**, implement **interface + no-op adapter** and mark task scope as “interface only” in PR title.
- WAL / backup expectations.

## Acceptance criteria

- [ ] Trace round-trip: evaluate (persist) → trace command retrieves identical trace events.
- [ ] Migration idempotent per project standards.

## Implementation notes

Align with planning SQLite `user_version` policy — avoid conflicting migration numbers.
