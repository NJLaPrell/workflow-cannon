# T860 — Implement deterministic CAE evaluation engine

| Field | Value |
| --- | --- |
| **Task ID** | T860 |
| **Engine `dependsOn`** | T841, T843, T844, T858, T859 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Pure **evaluate(context, registry, activations, mode)** → **effective bundle** + **trace** per T843/T846/T848; implement conflict **merge/shadow/fail** exactly as spec.

## Scope

**In:** Core evaluator, golden tests from T854 plan, trace emission.  
**Out:** CLI evaluate (T862), shadow labels (T863), enforcement (T866).

## Deliverable artifacts

1. **`evaluateActivationBundle`** (name TBD) with deterministic ordering.
2. **Golden files** under `test/cae/golden/*.json`.
3. **Property tests** (optional) for idempotence: same inputs → same bundle hash.

## Decisions to resolve

- Internal representation for “candidate activation” before merge.
- Whether trace is **pure** (replayable from inputs) or includes wall-clock (prefer monotonic `stepIndex`).

## Acceptance criteria

- [ ] All T843 worked examples have automated tests.
- [ ] Same-type conflict behaviors covered: merge, shadow, fail.

## Implementation notes

Keep evaluator **free of** `process.env` except via injected config for testability.
