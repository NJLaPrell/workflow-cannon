# T854 — CAE test strategy & coverage plan

| Field | Value |
| --- | --- |
| **Task ID** | T854 |
| **Engine `dependsOn`** | T840, T842, T843, T846 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Define **test layers**: pure evaluator golden vectors, registry validation, CLI integration, shadow/enforcement flags, schema migration compatibility.

## Scope

**In:** Test plan doc, fixture layout, CI gates, coverage priorities.  
**Out:** Writing every test (T869 + inline tasks).

## Deliverable artifacts

1. **`.ai/cae/test-plan.md`** with directory layout e.g. `test/cae/fixtures/`.
2. **Golden vector catalog**: input context + registry + expected bundle hash or full JSON.
3. **Risk map**: which untested edge cases block enforcement ship.

## Decisions to resolve

- Snapshot stability policy (OS paths normalization).
- Whether to hash bundle vs full JSON compare.
- E2E: spawn `wk run` vs direct handler calls.

## Acceptance criteria

- [ ] T860/T861/T862/T869 owners can execute plan without reinterpretation.
- [ ] Explicit **determinism** tests required before T866.

## Implementation notes

User rule: fixing tests without product code change is OK; don’t change production code only to satisfy tests without confirmation — for CAE **new** code, tests drive spec compliance.
