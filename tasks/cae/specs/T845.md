# T845 — CAE persistence & migration design ADR

| Field | Value |
| --- | --- |
| **Task ID** | T845 |
| **Engine `dependsOn`** | T837 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Decide what is **persisted** (traces, shadow metrics, ack state, registry snapshots), **where** (planning SQLite vs separate files vs new DB), retention, migrations, backup stance.

## Scope

**In:** ADR with schema sketch, migration numbering, rollback; interaction with existing `tasks.sqliteDatabaseRelativePath` / planning `user_version`.  
**Out:** Applying migrations (T867).

## Deliverable artifacts

1. **ADR** with table list (proposed): `cae_traces`, `cae_shadow_runs`, etc. — or explicit **file-only v1** decision.
2. **Retention policy** defaults and env overrides.
3. **Threat notes**: trace leakage of paths/secrets.

## Decisions to resolve

- **Ephemeral traces OK for v1?** If yes, document interface for T867 “no-op persistence adapter.”
- Co-locate with planning DB vs standalone `cae.sqlite`.
- Whether shadow **usefulness** ratings are stored.

## Acceptance criteria

- [ ] T867 implementer has unambiguous DDL or explicit deferral.
- [ ] Doctor hook expectations described (health signal).

## Accuracy notes (Phase 70 triage)

- **Reconcile with T846** before locking retention: trace payload size drives storage choice (ephemeral v1 vs SQLite).
- **Coexistence:** If tables live in planning SQLite, follow existing **`user_version`** / migration patterns; do not invent a parallel DB without ADR rationale.

## Implementation notes

Align with project preference for deterministic replay: store **registry hash** + **context hash** with trace when persisting.
