# T858 — Implement CAE registry loader & validation

| Field | Value |
| --- | --- |
| **Task ID** | T858 |
| **Engine `dependsOn`** | T839, T840, T857 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Load **artifact registry** + **activation definition** files, validate against normative schemas, emit stable **error codes** — **no evaluation** yet.

## Scope

**In:** Core library module (path from T837), unit tests for valid/invalid fixtures, optional doctor hook stub.  
**Out:** Context builder (T859), evaluator (T860).

## Deliverable artifacts

1. **Source**: loader + validator functions with typed exports.
2. **Fixtures**: `test/cae/fixtures/registry-invalid-*.json` etc.
3. **Error code enum** documented in `.ai/cae/error-codes.md` (short table).

## Decisions to resolve

- Package placement: `src/modules/<id>/` vs `src/core/cae/`.
- Single merged registry file vs directory glob — follow T839.

## Acceptance criteria

- [ ] Invalid registry fails with stable `code` (for future remediation catalog).
- [ ] Valid T857 seed loads in tests.

## Implementation notes

Do not add `workspace-kit run` handlers here unless splitting makes review harder — T861 may import this package.
