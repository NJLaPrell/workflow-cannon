# T844 — Acknowledgement model specification

| Field | Value |
| --- | --- |
| **Task ID** | T844 |
| **Engine `dependsOn`** | T837, T843 |
| **Program context** | [`../CAE-PROGRAM-CONTEXT.md`](../CAE-PROGRAM-CONTEXT.md) |

## Purpose

Define **acknowledgement strengths** and how they surface on the bundle and traces — **orthogonal** to Tier A/B **`policyApproval`** (`.ai/POLICY-APPROVAL.md`).

## Scope

**In:** Model doc, bundle fields for `pendingAcknowledgements`, completion semantics for `satisfy_required`.  
**Out:** Persistence of ack state (optional v1 — decide), enforcement wiring (T866).

## Deliverable artifacts

1. **Spec** `.ai/cae/acknowledgement-model.md`.
2. **JSON fragment schema** for `pendingAcknowledgements` items (merge into bundle schema).
3. **Comparison table**: ack vs `policyApproval` vs code gate — when each may block.

## Decisions to resolve

- Are acks **session-only** in v1 or persisted (task row, CAE table)?
- Can `ack_required` **block** in narrow enforcement lane, or only post–shadow bake?
- Machine-checkable `satisfy_required` vs human-only.

## Acceptance criteria

- [ ] No ambiguity that CAE replaces existing approval JSON paths.
- [ ] Each strength has one example in bundle JSON.

## Implementation notes

Enforcement task T866 must cite this spec verbatim for allowlisted behaviors.
