# CAE read-only CLI contract (v1) — artifact pointer

**Normative spec:** **`.ai/cae/cli-read-only.md`** (**`T847`**).

**JSON Schema (argv + success `data`):**

- `schemas/cae/cli-read-only-requests.v1.json`
- `schemas/cae/cli-read-only-data.v1.json`

**Handlers:** **`T861`** (registry list/get), **`T862`** (evaluate / explain / health / conflicts / trace).
