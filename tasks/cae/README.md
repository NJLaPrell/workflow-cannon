# CAE program (task engine)

**Program context:** [`CAE-PROGRAM-CONTEXT.md`](./CAE-PROGRAM-CONTEXT.md)

**Machine canon:** **`.ai/cae/`** (operator paths, schemas, ADRs). **Task state:** `pnpm exec wk run list-tasks '{"phaseKey":"70"}'`.

**Artifacts under [`artifacts/`](./artifacts/):** schema stubs (evolved into `schemas/cae/`), v1 CLI contract markdown, inventory notes for registry seed (**`T857`**).

Phase 70 design tasks **T837–T869** and follow-on rows are **completed** in the task engine; per-task specs and one-shot bootstrap scripts were removed after closeout.
