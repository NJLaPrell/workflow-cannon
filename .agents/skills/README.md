# Agent Skills

No `.agents/skills/<name>/SKILL.md` workspace skills are currently required. This directory is present so discovery can distinguish an intentional empty skill surface from a missing path.
