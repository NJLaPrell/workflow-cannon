---
description: Merged maintainer delivery — task loop, branching, commits, PR/merge (always on)
alwaysApply: true
---

# Maintainer delivery workflow (canonical)

Full numbered loop (branch → implement → validate → PR → merge → **`run-transition`**), branching/tagging, commit strategy, and PR expectations live in **`docs/maintainers/playbooks/task-to-main.md`** (single-task playbook) and **`docs/maintainers/AGENTS.md`** (task execution + CLI-first execution). Attach **`@task-to-main.md`** when driving one **`T###`** to **`main`**.

## Non-negotiables

- **Git + GitHub** own branch, PR, review, merge. **`workspace-kit`** owns task lifecycle and evidence in the configured task store (default SQLite under **`.workspace-kit/tasks/`**). Do not treat merged PRs as task **`complete`** without the matching Tier A **`run-transition`** when the task is in scope.
- **Blocked?** Record the blocker in task-engine state first; the SQLite/JSON task store is authoritative over chat.

Optional requestable rule: **`.cursor/rules/playbook-task-to-main.mdc`**.
