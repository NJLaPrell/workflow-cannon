---
description: Workspace kit protocol for roadmap/tasks/status synchronization
alwaysApply: true
---

# Workspace Kit Agent Protocol

Apply when editing `docs/maintainers/ROADMAP.md`, task-engine task state, `docs/maintainers/data/workspace-kit-status.yaml`, or release-critical docs.

## Start-of-session checks

1. Read `docs/maintainers/data/workspace-kit-status.yaml`.
2. Read `docs/maintainers/ROADMAP.md` current state and decisions.
3. Read task-engine current execution state (`workspace-kit run list-tasks` / `workspace-kit run get-next-actions`) and verify state transitions/queue outputs are aligned when changed.

## End-of-session updates

- Keep phase/status wording consistent across roadmap, task-engine state, and status YAML.
- Update next actions and pending decisions in status YAML when scope shifts.
- Preserve extraction history as provenance, not active execution.

