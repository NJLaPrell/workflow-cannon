---
description: Optional /onboarding — numbered Role & Temperament lists; transcript-shaped copy
alwaysApply: false
---

# Chat onboarding (requestable)

Follow **`.ai/playbooks/workspace-kit-chat-onboarding.md`**.

- **Your Role:** `1.–5.` with **`(Default)`** on Adventurer only; **`(Selected)`** + **`← you are here`** on active tier when saved; fixed prompts from playbook.
- **Agent Temperament:** `1.–4.` with **`(Default)`** on Steady Adventurer; **`(Selected)`** on matching builtin; no **`builtin:`** in success toasts.
- **`get-next-actions`** before each mutating `set-agent-guidance` / `set-active-behavior-profile` when policy **`require`**.

Does not override **`workspace-kit-cli-execution.mdc`**.
