{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://workflow-cannon.dev/schemas/cae/effective-activation-bundle.v1.json",
  "title": "CAE effective activation bundle (v1)",
  "description": "Deterministic evaluation output (T843). pendingAcknowledgements item detail: T844. Builder: T860.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "bundleId",
    "families",
    "pendingAcknowledgements",
    "conflictShadowSummary",
    "traceId"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "bundleId": {
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "description": "Deterministic id from canonical context + registry snapshot (T842 hashing)."
    },
    "resolvedAt": {
      "type": "string",
      "maxLength": 40,
      "description": "ISO-8601 timestamp when bundle was produced (optional)."
    },
    "families": {
      "type": "object",
      "additionalProperties": false,
      "required": ["policy", "think", "do", "review"],
      "properties": {
        "policy": {
          "type": "array",
          "items": { "$ref": "#/$defs/resolvedActivation" }
        },
        "think": {
          "type": "array",
          "items": { "$ref": "#/$defs/resolvedActivation" }
        },
        "do": {
          "type": "array",
          "items": { "$ref": "#/$defs/resolvedActivation" }
        },
        "review": {
          "type": "array",
          "items": { "$ref": "#/$defs/resolvedActivation" }
        }
      }
    },
    "pendingAcknowledgements": {
      "type": "array",
      "description": "Ack slots surfaced on bundle; semantics T844.",
      "items": { "$ref": "#/$defs/pendingAcknowledgementItem" }
    },
    "conflictShadowSummary": { "$ref": "#/$defs/conflictShadowSummary" },
    "traceId": {
      "type": "string",
      "minLength": 8,
      "maxLength": 128
    },
    "explanationRef": {
      "type": "string",
      "maxLength": 256,
      "description": "Opaque handle for explain / health (T846/T862)."
    }
  },
  "$defs": {
    "resolvedActivation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["activationId", "family", "priority", "aggregateTightness", "artifactIds"],
      "properties": {
        "activationId": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"
        },
        "family": {
          "type": "string",
          "enum": ["policy", "think", "do", "review"]
        },
        "priority": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9999
        },
        "aggregateTightness": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255,
          "description": "From .ai/cae/precedence-merge.md (lower = tighter)."
        },
        "artifactIds": {
          "type": "array",
          "minItems": 1,
          "maxItems": 64,
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"
          }
        }
      }
    },
    "pendingAcknowledgementItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["activationId", "strength"],
      "properties": {
        "activationId": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"
        },
        "strength": {
          "type": "string",
          "enum": [
            "none",
            "surface",
            "recommend",
            "ack_required",
            "satisfy_required"
          ]
        },
        "ackToken": {
          "type": "string",
          "maxLength": 128,
          "description": "Session correlation id; persistence optional (T867)."
        },
        "machineCheckable": {
          "type": "boolean",
          "description": "If true, satisfy_required may use structured evidence (T860/T866)."
        },
        "blockingLane": {
          "type": "string",
          "enum": ["none", "advisory", "enforcement"],
          "description": "Where this ack may gate (T866)."
        },
        "surfaceCopyKey": {
          "type": "string",
          "maxLength": 128,
          "description": "Stable key for rendered copy; no raw NL in bundle."
        }
      }
    },
    "conflictShadowSummary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["evalMode", "entries"],
      "properties": {
        "evalMode": {
          "type": "string",
          "enum": ["live", "shadow"]
        },
        "entries": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kind", "activationIds", "resolution"],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "policy_vs_advisory",
                  "same_family_tie",
                  "policy_contradiction"
                ]
              },
              "activationIds": {
                "type": "array",
                "minItems": 2,
                "maxItems": 32,
                "items": {
                  "type": "string",
                  "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"
                }
              },
              "resolution": {
                "type": "string",
                "enum": [
                  "policy_wins",
                  "merge",
                  "shadow",
                  "fail_explicit"
                ]
              },
              "detail": {
                "type": "string",
                "maxLength": 512
              }
            }
          }
        }
      }
    }
  }
}
