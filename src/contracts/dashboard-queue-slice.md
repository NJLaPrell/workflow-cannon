# Dashboard Queue Slice Command

Provides a fast snapshot of the dashboard queue without loading full task state. Ideal for hot reads.

**Command name:** `dashboard-queue-slice`

**Implementation:** Handled by `dashboardQueueSliceCommand` in `task-engine-dashboard-on-command.ts`.
