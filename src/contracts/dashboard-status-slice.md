# Dashboard Status Slice Command

Provides a fast snapshot of the dashboard status without loading the full system state. Ideal for hot reads.

**Command name:** `dashboard-status-slice`

**Implementation:** Handled by `dashboardStatusSliceCommand` in `task-engine-dashboard-on-command.ts`.
