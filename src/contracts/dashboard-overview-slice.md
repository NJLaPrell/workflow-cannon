# Dashboard Overview Slice Command

This command provides a minimal startup‑safe overview of the dashboard. It builds only the essential data required for the overview slice without loading full system status or task state.

**Command name:** `dashboard-overview-slice`

**Implementation:** Handled by `dashboardOverviewSliceCommand` in `task-engine-dashboard-on-command.ts`.
