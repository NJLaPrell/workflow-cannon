# Dashboard Agent Types Slice Command

Provides a fast snapshot of agent types on the dashboard without loading full system data. Ideal for hot reads.

**Command name:** `dashboard-agent-types-slice`

**Implementation:** Handled by `dashboardAgentTypesSliceCommand` in `task-engine-dashboard-on-command.ts`.
