# Dashboard Agent Activity Slice Command

Provides a fast snapshot of agent activity on the dashboard without loading full system data. Ideal for hot reads.

**Command name:** `dashboard-agent-activity-slice`

**Implementation:** Handled by `dashboardAgentActivitySliceCommand` in `task-engine-dashboard-on-command.ts`.
