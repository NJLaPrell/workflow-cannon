# Skills module

Skill pack discovery and application (Claude Code–shaped `.claude/skills/<id>/SKILL.md`).

Config keys live in workspace **`skills.*`** (see `skills.discoveryRoots` in config registry).
