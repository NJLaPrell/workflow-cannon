# list-skills

```bash
workspace-kit run list-skills '{}'
```

Lists discovered packs under `skills.discoveryRoots` (default `.claude/skills`). Each row includes `id`, `version`, `displayName`, `description`, `discoveryTags`, and paths. See `docs/maintainers/adrs/ADR-skill-packs-v1.md`.
