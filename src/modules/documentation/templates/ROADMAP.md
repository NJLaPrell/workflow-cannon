# ROADMAP (data-driven)

Maintainer-facing **`docs/maintainers/ROADMAP.md`** is assembled from JSON + markdown under **`src/modules/documentation/data/`** by **`workspace-kit run generate-document`** (`documentType` **`ROADMAP.md`**). This file is not used as an AI template for roadmap body text.
