# Ideas Module

Stores lightweight operator ideas in kit SQLite for later planning-chat workflows. The module owns the `workflow_ideas` table and keeps idea persistence separate from task-engine tasks.
