# Context activation module (CAE)

**`cae-*`** commands: registry inspection (**`T861`**) + evaluation (**`T862`**) + SQLite registry admin mutators (**Phase 70** — see **`.ai/cae/registry-mutation-governance.md`**). Read-only paths have no `policyApproval`; admin mutators use **`caeMutationApproval`** + **`kit.cae.adminMutations`**.

See **`.ai/cae/cli-read-only.md`** and **`.ai/cae/README.md`**.
