# Context activation module (CAE)

Read-only **`cae-*`** commands for registry inspection (**`T861`**) and evaluation (**`T862`**). Advisory only — no `policyApproval` on read-only commands.

See **`.ai/cae/cli-read-only.md`** and **`.ai/cae/README.md`**.
