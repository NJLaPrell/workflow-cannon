<!--
agentCapsule|v=1|command=update-agent-definition|module=task-engine|schema_only=pnpm exec wk run update-agent-definition --schema-only '{}'
-->

# update-agent-definition

Update an existing AgentDefinition v1 bridge row.
