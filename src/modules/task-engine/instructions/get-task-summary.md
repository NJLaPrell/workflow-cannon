<!--
agentCapsule|v=1|command=get-task-summary|module=task-engine|schema_only=pnpm exec wk run get-task-summary --schema-only '{}'
-->

# get-task-summary

Return active-task lifecycle summary for dashboard and automation clients.

## Usage

```
workspace-kit run get-task-summary '{}'
```
