<!--
agentCapsule|v=1|command=get-agent-definition|module=task-engine|schema_only=pnpm exec wk run get-agent-definition --schema-only '{}'
-->

# get-agent-definition

Read one AgentDefinition v1 bridge record.
