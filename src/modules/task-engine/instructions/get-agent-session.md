<!--
agentCapsule|v=1|command=get-agent-session|module=task-engine|schema_only=pnpm exec wk run get-agent-session --schema-only '{}'
-->

# get-agent-session

Read one AgentSession v1 row by id.

## Usage

```bash
workspace-kit run get-agent-session '{"sessionId":"<uuid>"}'
```
