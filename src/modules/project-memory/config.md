# project-memory module config

Operational memory lives under `.workspace-kit/memory/records.json` (machine store) and optional root `CANNON.md` (human index). It does not replace `.ai/` machine canon or documentation-module outputs.
