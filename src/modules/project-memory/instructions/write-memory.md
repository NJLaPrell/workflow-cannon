<!--
agentCapsule|v=1|command=write-memory|module=project-memory|schema_only=pnpm exec wk run write-memory --schema-only '{}'
-->

# write-memory

Create or update a **draft** memory record. Requires `category` and `body`. Optional `id` to update an existing record.
