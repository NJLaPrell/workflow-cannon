<!--
agentCapsule|v=1|command=prune-memory|module=project-memory|schema_only=pnpm exec wk run prune-memory --schema-only '{}'
-->

# prune-memory

Mark a record **pruned** with an audit trail. Requires `id` and `auditNote`.
