<!--
agentCapsule|v=1|command=approve-memory|module=project-memory|schema_only=pnpm exec wk run approve-memory --schema-only '{}'
-->

# approve-memory

Promote a draft record to **approved**. Requires `id`.
