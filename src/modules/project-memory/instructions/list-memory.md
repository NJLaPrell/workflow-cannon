<!--
agentCapsule|v=1|command=list-memory|module=project-memory|schema_only=pnpm exec wk run list-memory --schema-only '{}'
-->

# list-memory

Read project-memory records from `.workspace-kit/memory/records.json`.

Optional filters: `status` (`draft` | `approved` | `pruned`), `category` (string).
