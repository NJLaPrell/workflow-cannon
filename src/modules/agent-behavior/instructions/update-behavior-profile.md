# update-behavior-profile

```bash
workspace-kit run update-behavior-profile '{"profileId":"custom:acme","updates":{"summary":"…"}}'
```

Custom ids only. `updates` may include `label`, `summary`, `dimensions`, `interactionNotes`.
