<!--
agentCapsule|v=1|command=list-behavior-profiles|module=agent-behavior|schema_only=pnpm exec wk run list-behavior-profiles --schema-only '{}'
-->

# list-behavior-profiles

```bash
workspace-kit run list-behavior-profiles '{}'
```

Returns `data.profiles`: `{ id, label, kind }[]` for builtins and custom profiles.
