<!--
agentCapsule|v=1|command=get-behavior-profile|module=agent-behavior|schema_only=pnpm exec wk run get-behavior-profile --schema-only '{}'
-->

# get-behavior-profile

```bash
workspace-kit run get-behavior-profile '{"profileId":"builtin:balanced"}'
```

Returns resolved profile (including `extends` merge).
