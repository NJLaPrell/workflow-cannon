# list-subagents

```bash
workspace-kit run list-subagents '{}'
workspace-kit run list-subagents '{"includeRetired":true}'
```

Lists subagent definitions from kit SQLite (`user_version` 6+). See `docs/maintainers/ADR-subagent-registry-v1.md`.
