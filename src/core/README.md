# Core Kernel

Core runtime services and orchestration boundaries:

- lifecycle orchestration
- config resolution
- policy decision point
- event dispatch
- storage abstractions

Keep this layer framework-light and deterministic.
